[project]
name = "ozzydb"
version = "0.3.0"
description = "Python client for OzzyDB v4"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Riley Leff" }
]
keywords = ["data", "versioning", "reproducibility", "science", "artifacts"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
]

dependencies = [
    "polars>=1.0.0",
    "pyarrow>=15.0.0",
    "requests>=2.28.0",
    "blake3>=1.0.0",
]

[project.optional-dependencies]
pandas = ["pandas>=2.0.0"]
dev = ["pytest>=8.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ozzydb"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pandas>=2.3.3",
    "pytest>=9.0.2",
]
