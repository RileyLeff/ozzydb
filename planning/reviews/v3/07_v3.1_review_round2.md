# v3.1 Multi-Provider Compute Refactor — Review Round 2

**Date**: 2026-02-14
**Models**: Claude Opus 4.6 (Codex: usage limit hit; Gemini: E2BIG)
**Context**: ~377k tokens

## Verdict: CLEAN ROUND

The reviewer declared CLEAN ROUND. All major findings were self-withdrawn after deeper analysis. Two minor fixes applied.

## Findings

### Major (0 confirmed — 3 raised, all self-withdrawn)

- **M1 (withdrawn)**: `is_collection` always false — only affects collection inputs which aren't exercised in v3.1 scope. Pre-existing known limitation (n10 from round 1).
- **M2 (withdrawn)**: Docker env var injection via `-e` flag — safe because `tokio::process::Command` doesn't use shell.
- **M3 (withdrawn)**: `compute_waves` edge parsing for `data:raw` — safe because `"data:raw".split('.').next()` returns `"data:raw"`, not `"data"`.

### Minor (2 fixed, 4 accepted)

- **m3**: Source tarball not cleaned up if `compute_waves()` fails. **Fixed** — added explicit cleanup on error.
- **m4**: `curl -s` on output upload suppresses error details. **Fixed** — changed to `curl -sSf`.
- m2: Redundant DB query in `resolve_input_content_type`. **Accepted** — performance only.
- m5: Secrets hash computed twice per node. **Accepted** — performance only.
- m6: Output temp key may leak on panic. **Accepted** — rare edge case, periodic cleanup would handle.
- m7 (withdrawn): `fly_backend()` downcast — safe because `ComputeBackend: Any` preserves TypeId.

### Notes (3)

- n1: Docker containers have no network isolation (known tradeoff — containers need network for presigned URL I/O)
- n2: PlatformFingerprint runs on server, not container (known for Fly deployments)
- n3: Param name sanitization could cause collisions (low risk — only dashes stripped)

## Status

Round 2 CLEAN. Need one more clean round for convergence (2 consecutive clean rounds required).
